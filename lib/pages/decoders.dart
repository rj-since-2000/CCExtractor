import 'package:flutter/material.dart';

class Decoders extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Center(
        child: Text(
          'TODO: decoders',
          style: TextStyle(fontSize: 28),
        ),
      ),
    );
  }
}
