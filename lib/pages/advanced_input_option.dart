import 'package:flutter/material.dart';

class AdvancedInputOption extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Center(
        child: Text(
          'TODO: advanced input option',
          style: TextStyle(fontSize: 28),
        ),
      ),
    );
  }
}
